# Agent Rules

- When you implement changes, always propose 2-4 commit message options.
- Avoid magic values in code: extract them into named constants. Tests can use inline literals when that keeps assertions clearer.
